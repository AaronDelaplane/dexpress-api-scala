version: '3.7'
services:
#  dexpress-inventory-service:
#    image: dexpress-inventory-service:latest
#    ports:
#      - 10000:10000
  postgres:
    image: postgres:12.1
    restart: "no"
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
      POSTGRES_DB: inventory
#    volumes:
#      - empty_dir:/var/lib/postgresql/data
#      - ./1.sql:/docker-entrypoint-initdb.d/init.sql  
#volumes:
#  empty_dir:
    